interface:
  display_name: "YCT Direct"
  short_description: "Explicit no-subagent mode"
  default_prompt: "Use $yct-direct to handle this task in the main thread."
policy:
  allow_implicit_invocation: false
